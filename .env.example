# ==============================================================================
# Pipeline as Code: Environment Configuration Template
# Copy this file to .env and adjust variables for your local or production setup.
# DO NOT commit real secrets (.env) to version control.
# ==============================================================================

# Database Connection (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=earbuds_db
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Kafka Message Broker
KAFKA_SERVER=localhost:9092
KAFKA_TOPIC=earbuds_events

# Airflow Orchestrator
AIRFLOW_URL=http://localhost:8080
AIRFLOW_DAG_ID=earbuds_rough_to_staging
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=your_airflow_password_here

# Pipeline Settings
BATCH_SIZE=500
LOG_LEVEL=INFO

